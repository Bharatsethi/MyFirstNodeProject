import React from 'react';
import './Contact.css';

const ContactComp = () => {
    return (
        <div className="contact">
            {/* contact content */}
        </div>
    );
};

export default ContactComp;